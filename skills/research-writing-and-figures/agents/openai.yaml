interface:
  display_name: "Research Writing & Figures"
  short_description: "Route tasks that need both figure reading and scientific writing"
  default_prompt: "Use $research-writing-and-figures to coordinate a manuscript rewrite that depends directly on reading the figure panels."

policy:
  allow_implicit_invocation: true
